package main

import "fmt"

// An array is a numbered sequence of elements of specific length
// THe type of elemends and the length are both
// part of the arrays type
// by default an array is zero-valued

func main() {
	// syntax a little funky to decalare and initialize
	a := [5]int{1, 2, 3, 4, 5}
	fmt.Println(a)

	// usually going to be working with slices
}
